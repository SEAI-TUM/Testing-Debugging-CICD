# Exercise 4 — Continuous Integration with GitHub Actions.
#
# This workflow is INCOMPLETE. Fill in the TODOs so that every
# push and every pull request automatically installs the dependencies and
# runs the test suite with coverage.
#
# Docs: https://docs.github.com/actions

name: CI

# TODO 1: trigger the workflow on push and on pull_request.
on:
  # push:
  #   branches: [ main ]
  # pull_request:
  workflow_dispatch:   # (manual trigger, so the file is valid before you start)

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      # TODO 2: check out the repository (actions/checkout@v4).

      # TODO 3: set up Python 3.12 (actions/setup-python@v5).

      # TODO 4: install dependencies from requirements.txt.
      #   run: pip install -r requirements.txt

      # TODO 5: run the tests with coverage.
      #   run: pytest --cov=src --cov-report=term-missing
      - name: Placeholder
        run: echo "Replace these steps — see the TODOs and solutions/ci_solution.yml"
